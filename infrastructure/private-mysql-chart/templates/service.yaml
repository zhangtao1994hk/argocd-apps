apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: middleware
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
    - name: metrics
      port: 9104
      targetPort: 9104
  selector:
    app: mysql
