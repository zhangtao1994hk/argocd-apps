apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: middleware
type: Opaque
stringData:
  # 1. 数据库 root 密码
  MYSQL_ROOT_PASSWORD: "777777"
  # 2. Exporter 连接数据库的配置 (Sidecar 模式下直接连 127.0.0.1)
  DATA_SOURCE_NAME: "root:777777@(127.0.0.1:3306)/"
