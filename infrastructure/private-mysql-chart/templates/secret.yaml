apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: middleware
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "777777"
  # TODO(prod): 创建专用监控用户并替换以下凭证，限制权限为 PROCESS, REPLICATION CLIENT, SELECT on *.*
  .my.cnf: |
    [client]
    user=root
    password=777777
    host=127.0.0.1
    port=3306
